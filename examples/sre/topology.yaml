# Generated by /discover — service dependency graph from env var inspection
discovered_at: "2026-04-07T15:50:00Z"
method: env_vars

services:
  frontend:
    upstream: [loadgenerator]
    downstream: [productcatalogservice, currencyservice, cartservice, recommendationservice, shippingservice, checkoutservice, adservice]
  checkoutservice:
    upstream: [frontend]
    downstream: [productcatalogservice, shippingservice, paymentservice, emailservice, currencyservice, cartservice]
  cartservice:
    upstream: [checkoutservice, frontend]
    downstream: [redis-cart]
  adservice:
    upstream: [frontend]
    downstream: []
  redis-cart:
    upstream: [cartservice]
    downstream: []
